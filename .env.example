# Copia este archivo como ".env" en el VPS (NO lo subas nunca a git; .env ya está en .gitignore)
# Para el primer despliegue urgente, usa aquí las mismas credenciales de Supabase que ya están
# en facilcomanda-backend/.env (DB_HOST, DB_USER, DB_PASSWORD, etc.). No copies aqui valores reales.
# Migrar la base de datos a un servidor propio es un paso posterior, no bloquea este despliegue.

DB_HOST=
DB_PORT=5432
DB_NAME=
DB_U