services:
  backend:
    build: .
    container_name: facilcomanda-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: 8080
    ports:
      - "127.0.0.1:8080:8080"